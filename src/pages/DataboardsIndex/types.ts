// Data Board categories use the same model as /schemas (nested `subCategories`,
// `type: 'databoard'`). Boards carry `category_id`; the category does not list boards.
export type { SchemaCategory } from '@/pages/BoardSchema/types';
